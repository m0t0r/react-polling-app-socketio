'use strict';

let React = require('react');

const Speaker = React.createClass({
  render() {
    return (
      <div>
        <h1>Speaker</h1>
      </div>
    );
  }
});

module.exports = Speaker;
